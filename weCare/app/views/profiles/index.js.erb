
var locationDataLat = $('#map').attr("data-latitude");
var locationDataLng = $('#map').attr("data-longitude");
// var locationDistance = $('#map').attr("data-distance");
console.log(locationDataLat);

    var mapPlaceholder = $("#map");
    var latLong = {lat: mapPlaceholder.data('latitude'), lng: mapPlaceholder.data('longitude')};
    var map = new google.maps.Map(mapPlaceholder[0], {
    center: latLong,
    zoom: 10
    });
var marker = new google.maps.Marker({
  position: {lat: parseFloat(locationDataLat), lng: parseFloat(locationDataLng)},
  map: map,
  title: "Your location"
});
<% @nearby_profiles.each do |profile| %>
var marker = new google.maps.Marker({
  position: {lat: <%=profile.latitude%>, lng: <%=profile.longitude%>},
  // map: map,
  title: 'Hello World!'
});
  marker.setMap(map);
<% end %>

  $('.nearby_babysitters').html("<%= j(render @nearby_profiles) %>")
